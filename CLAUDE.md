# Claude Instructions

Follow `AGENTS.md`.

Do not run:

```bash
npm run lint
npm run build
```

The project owner will run those commands.

Before changing Next.js behavior, read the relevant local documentation in `node_modules/next/dist/docs/`.
